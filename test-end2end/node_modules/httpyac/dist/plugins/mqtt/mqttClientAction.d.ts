import * as models from '../../models';
export declare class MQTTClientAction implements models.HttpRegionAction {
    id: models.ActionType;
    process(context: models.ProcessorContext): Promise<boolean>;
    private requestMQTT;
    private subscribeToTopics;
    private publishToTopics;
    private toQoS;
    private toMergedHttpResponse;
    private isMQTTError;
}
