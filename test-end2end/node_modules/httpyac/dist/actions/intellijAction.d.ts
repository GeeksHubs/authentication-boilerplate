import * as models from '../models';
export interface IntellijScriptData {
    fileName: string;
}
export declare class IntellijAction implements models.HttpRegionAction {
    private readonly scriptData;
    id: models.ActionType;
    constructor(scriptData: models.ScriptData | IntellijScriptData);
    process(context: models.ProcessorContext): Promise<boolean>;
    private loadScript;
    private isIntellijScriptData;
}
