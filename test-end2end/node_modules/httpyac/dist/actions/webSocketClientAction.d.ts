import * as models from '../models';
export declare class WebSocketClientAction implements models.HttpRegionAction {
    id: models.ActionType;
    process(context: models.ProcessorContext): Promise<boolean>;
    private requestWebsocket;
    private toMergedHttpResponse;
    private toStringBody;
    private toHttpResponse;
    private isWebsocketError;
}
