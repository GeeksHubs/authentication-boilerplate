import * as models from '../models';
import { HookInterceptor, HookTriggerContext } from 'hookpoint';
export declare class CreateRequestBodyInterceptor implements HookInterceptor<[models.ProcessorContext], boolean | void> {
    private readonly rawBody;
    constructor(rawBody: Array<string | models.RequestBodyImport>);
    beforeTrigger(hookContext: HookTriggerContext<[models.ProcessorContext], boolean | undefined>): Promise<boolean | undefined>;
    private normalizeBody;
    private formUrlEncodedJoin;
}
