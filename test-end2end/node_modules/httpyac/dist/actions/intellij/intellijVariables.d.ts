import * as models from '../../models';
import { Variables as JetbrainsVariables } from './http-client';
export declare class IntellijVariables implements JetbrainsVariables {
    private readonly context;
    private userSession;
    constructor(context: models.ProcessorContext);
    private getIntellijSession;
    private isIntellijGlobalCacheSession;
    set(varName: string, varValue: string): void;
    get(varName: string): unknown;
    isEmpty(): boolean;
    clear(varName: string): void;
    clearAll(): void;
}
