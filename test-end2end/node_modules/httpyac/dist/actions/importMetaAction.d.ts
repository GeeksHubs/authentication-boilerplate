import * as models from '../models';
export interface ImportProcessorContext extends models.ProcessorContext {
    options: {
        httpFiles?: Array<models.HttpFile>;
    };
}
export declare class ImportMetaAction implements models.HttpRegionAction {
    private readonly fileName;
    private readonly httpFileStore;
    id: models.ActionType;
    constructor(fileName: string, httpFileStore: models.HttpFileStore);
    process(context: ImportProcessorContext): Promise<boolean>;
}
