import { ActionType, HttpRegionAction, ProcessorContext } from '../models';
export declare class DefaultHeadersAction implements HttpRegionAction {
    private readonly data;
    private readonly setHeaders;
    id: ActionType;
    constructor(data: string, setHeaders: (headers: Record<string, unknown>, context: ProcessorContext) => void);
    process(context: ProcessorContext): Promise<boolean>;
}
