import { ActionType, HttpRegionAction } from '../models';
import { ImportProcessorContext } from './importMetaAction';
export interface RefMetaHttpRegionData {
    name: string;
    force: boolean;
}
export declare class RefMetaAction implements HttpRegionAction {
    private readonly data;
    id: ActionType;
    constructor(data: RefMetaHttpRegionData);
    process(context: ImportProcessorContext): Promise<boolean>;
}
