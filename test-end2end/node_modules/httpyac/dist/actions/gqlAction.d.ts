import { ActionType, HttpRegionAction, ProcessorContext } from '../models';
export declare type GqlLoadData = string | ((context: ProcessorContext) => Promise<string | undefined>);
export interface GqlData {
    operationName?: string;
    query?: GqlLoadData;
    fragments: Record<string, GqlLoadData>;
}
export interface GqlPostRequest {
    query: string;
    operationName?: string;
    variables?: Record<string, unknown>;
}
export declare class GqlAction implements HttpRegionAction {
    private readonly gqlData;
    id: ActionType;
    before: ActionType[];
    constructor(gqlData: GqlData);
    process(context: ProcessorContext): Promise<boolean>;
}
